<div class="languages">
  <ul>
    {% for item in items %}
      <li class="item {{- item.active ? ' current-menu-item' }}">
        <a class="link" href="{{ item.url|e('esc_url') }}">{{ item.native_name }}</a>
      </li>
    {% endfor %}
  </ul>
</div>
